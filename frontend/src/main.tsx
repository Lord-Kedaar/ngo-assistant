import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import'./style.css';
type S={filename:string;section:string;label:string;score:number};const ex=['Jak rozpocząć wolontariat?','Jakie dokumenty musi podpisać nowy wolontariusz?','Kto zatwierdza koszt wydarzenia?','Jak rozliczyć zakup materiałów?','Jak wypożyczyć projektor?','Kto może publikować posty w social media?','Jak zgłosić pomysł warsztatu?','Co powinno znaleźć się w dokumentacji grantowej?'];
function App(){const[q,setQ]=useState(''),[ans,setAns]=useState(''),[src,setSrc]=useState<S[]>([]),[rem,setRem]=useState(5),[load,setLoad]=useState(false),[on,setOn]=useState(false),[err,setErr]=useState('');useEffect(()=>{fetch('/api/status').then(r=>r.json()).then(d=>setOn(d.model_available));fetch('/api/quota').then(r=>r.json()).then(d=>setRem(d.remaining))},[]);async function ask(t=q){setLoad(true);setErr('');setAns('');try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:t,website:''})});const d=await r.json();if(!r.ok)throw Error(d.detail||'Błąd');setAns(d.answer);setSrc(d.sources||[]);setRem(d.remaining)}catch(e:any){setErr(e.message)}finally{setLoad(false)}}return <><header className="hero"><span>Demonstrator portfolio</span><h1>Lokalny asystent wiedzy dla NGO</h1><p>Procedury, regulaminy i instrukcje w jednym miejscu — bez wysyłania dokumentów do publicznego chatbota.</p><div className="badges"><b>Dokumenty syntetyczne</b><b>Maksymalnie 5 pytań</b><b className={on?'ok':'bad'}>{on?'Model dostępny':'Model offline'}</b></div><a href="#demo">Wypróbuj asystenta</a></header><main><section><h2>Problem</h2><p>Dokumenty NGO bywają rozproszone, wolontariusze pytają ciągle o te same rzeczy, a procedury są trudne do odnalezienia.</p></section><section><h2>Jak to działa</h2><div className="cards"><div>1. Dokumenty indeksowane lokalnie</div><div>2. Pytanie porównywane z fragmentami</div><div>3. Model dostaje tylko właściwe fragmenty</div><div>4. Odpowiedź zawiera źródła</div></div></section><section><h2>Publiczny chatbot vs lokalny asystent</h2><div className="compare"><p>Publiczny chatbot: ogólna wiedza i ryzyko niekontrolowanego wklejania danych.</p><p>Lokalny asystent: wiedza z dokumentów organizacji i większa kontrola przepływu danych.</p></div><p>Lokalność nie jest magiczną tarczą. Bezpieczeństwo zależy od konfiguracji, uprawnień, aktualizacji i procedur.</p></section><section id="demo"><h2>Demo czatu</h2><p className="warn">Nie wpisuj danych osobowych, poufnych ani prawdziwych danych organizacji.</p><textarea maxLength={400} value={q} onChange={e=>setQ(e.target.value)} /><div className="row"><span>{q.length}/400 · pozostało: {rem}</span><button disabled={load||q.trim().length<3} onClick={()=>ask()}>{load?'Oczekiwanie…':'Wyślij'}</button></div>{err&&<p className="err">{err}</p>}{ans&&<article><p>{ans}</p>{src.map(s=><div className="source" key={s.label}>{s.filename} — {s.section}</div>)}</article>}</section><section><h2>Przykładowe pytania</h2><div className="chips">{ex.map(e=><button onClick={()=>{setQ(e);ask(e)}}>{e}</button>)}</div></section><section><h2>Architektura</h2><pre>Użytkownik → Tailscale Funnel → Fedora/FastAPI + RAG/SQLite → SSH tunnel → Mac Studio/oMLX</pre></section><section><h2>Co pokazuje demonstrator</h2><p>AI literacy, privacy-first, RAG, lokalne i hybrydowe wdrożenia, świadome ograniczanie zakresu.</p></section><section><h2>O autorze</h2><p>Radosław Pleskot — trener i konsultant bezpiecznych wdrożeń AI, lokalne modele, automatyzacje, PL / DE / EN.</p><a href="mailto:kontakt@example.org">Kontakt e-mail</a></section></main><footer>Demonstrator portfolio. Fundacja Mosty Sąsiedzkie jest organizacją fikcyjną. Dokumenty są syntetyczne. Nie wprowadzaj danych osobowych ani poufnych. To nie jest porada prawna. Brak analityki i trackerów marketingowych. <a href="/privacy">Prywatność</a></footer></>}
createRoot(document.getElementById('root')!).render(<App/>);